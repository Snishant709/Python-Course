#Virtual Enivronment is tool used to isolate specific python environments on a specific machine
# This is useful when working on project where we have different versions of packages and they are not compatible with each other
#Refer to the desktop saved file named Environmnets
''' 
Create a virtual environment
python -m venv myenv

# Activate the virtual environment (Windows)
myenv\Scripts\activate

# Deactivate the virtual environment
deactivate

#install all requirements if the use want to know all the packages used in it
first write all the packages present in your environment using 
'pip freeze > requirements.txt'

and if someone wants all that  packages installed in their machine then 
'pip install -r requirements.txt'
'''


